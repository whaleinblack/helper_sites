# Sites

每个子目录都是一个可独立安装、运行和部署的站点。

## 约定

- 目录名：小写英文和连字符，例如 `invoice-helper`。
- 必需文件：`README.md`、依赖清单、环境变量示例（需要时）。
- 禁止提交：`node_modules/`、构建产物、缓存、密钥、私有部署 ID。
- 站点之间不直接引用源码；确需共享的代码应先形成明确的共享包策略。
- 每次新增或移除站点，都同步更新根目录 `sites.json`。
